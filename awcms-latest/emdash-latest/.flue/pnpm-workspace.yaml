# Standalone pnpm workspace config for the triage bot. The repo root's
# pnpm-workspace.yaml doesn't list .flue as a workspace, and this file
# is what pnpm picks up when invoked from inside .flue/. Mirrors the
# relevant build-allow settings from the root config.

strictDepBuilds: false
allowBuilds:
  esbuild: true
  workerd: true
  "@google/genai": false
  "@mongodb-js/zstd": false
  "node-liblzma": false
  protobufjs: false
  sharp: false

overrides:
  # GHSA-gv7w-rqvm-qjhr (HIGH) + GHSA-g7r4-m6w7-qqqr (LOW): esbuild < 0.28.1
  # missing binary integrity check and dev-server arbitrary file read.
  # Applied as downstream security patch; re-check on next upstream sync.
  esbuild: ^0.28.1
