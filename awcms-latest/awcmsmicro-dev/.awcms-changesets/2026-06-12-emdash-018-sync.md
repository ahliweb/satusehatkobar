---
"@awcms-micro/template-default-cloudflare": minor
"@awcms-micro/template-default-example": minor
---

chore(templates): sync with EmDash 0.18.0 (migration 043, scheduled publishing, TaxonomyTerm hydration)

Synced both templates with EmDash 0.18.0 (upstream commit `ff5855ab`):

- **Migration 043** auto-runs on next boot, adding `_emdash_relations` and `_emdash_content_references` tables for the new content references feature
- **Cloudflare template `src/worker.ts`** updated to use `@emdash-cms/cloudflare/worker` entry point, which adds a `scheduled()` Cron Trigger handler for cron-driven scheduled publishing with edge-cache invalidation (closes #201)
- **`wrangler.jsonc`** gains `"triggers": { "crons": ["* * * * *"] }` so scheduled content auto-publishes every minute on production
- **Both template `emdash-env.d.ts`** updated: `TaxonomyTerm` imported, `terms?: Record<string, TaxonomyTerm[]>` added to all collection interfaces (terms are now hydrated directly onto entries — no extra `getEntryTerms()` call needed)
- **Lint fixes**: `e18e(prefer-array-to-sorted)` and `e18e(prefer-static-regex)` resolved in `index.astro`, `PortfolioCard.astro`, `services/index.astro`, `portfolio/index.astro`, `testimonials/index.astro`
- Downstream patch `0016-cloudflare-astro.patch` updated for new cloudflare package.json context
