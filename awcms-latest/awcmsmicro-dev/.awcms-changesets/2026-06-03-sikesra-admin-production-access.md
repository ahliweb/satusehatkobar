---
"@awcms-micro/plugin-sikesra": patch
---

Allow trusted EmDash admins to bootstrap SIKESRA admin access and fall back safely when production D1 SIKESRA tables are missing or still use transition-state schemas, so protected registry, custom attribute, audit, archive, delete request, access, ABAC, region, data type, and public status pages can load instead of failing.
